{
  "name": "logseq-clean-empty-pages",
  "version": "1.0.0",
  "description": "A Logseq plugin that cleans up empty pages and journals.",
  "main": "dist/index.js",
  "author": "ghzhang233",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/index.js",
    "watch": "esbuild index.js --bundle --outfile=dist/index.js --watch"
  },
  "dependencies": {
    "@logseq/libs": "latest"
  },
  "devDependencies": {
    "esbuild": "^0.18.0"
  },
  "logseq": {
    "id": "_zeglkzer2",
    "main": "dist/index.js"
  }
}